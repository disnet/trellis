// The public reading surface: server-rendered plain pages, no client
// JavaScript. The workspace app turns SSR off globally; everything under
// /garden turns it back on.
export const ssr = true;
export const csr = false;
